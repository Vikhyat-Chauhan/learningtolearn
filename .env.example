# This is a fully client-side, static flashcard deck — it needs NO runtime
# secrets (no database, auth, or external APIs). This file exists only to mirror
# the Hearth project's convention and document that fact.
#
# @vercel/analytics works automatically on Vercel with no key required.
